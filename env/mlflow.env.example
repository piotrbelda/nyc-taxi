# MLflow
MLFLOW_PORT=8000
MLFLOW_EXPERIMENT_NAME=taxi
MLFLOW_SCHEMA=mlflow
MLFLOW_TRACKING_URI=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?options=-c%20search_path%3D${MLFLOW_SCHEMA}

# AWS S3
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
S3_BUCKET_NAME=
